name: Deploy RecipeHub

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: SSH into EC2 and deploy
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script_stop: true
          script: |
            set -e

            echo ">>> Ensuring deployment directory exists..."
            mkdir -p ~/foodrecipe
            cd ~/foodrecipe

            # Pull Backend
            if [ ! -d "backend" ]; then
              git clone https://github.com/Keerthipriya910/recipehub-aws-backend.git backend
            else
              cd backend
              git pull
              cd ..
            fi

            # Pull Frontend
            if [ ! -d "frontend" ]; then
              git clone https://github.com/Keerthipriya910/recipehub-aws-frontend.git frontend
            else
              cd frontend
              git pull
              cd ..
            fi

            echo ">>> Installing dependencies..."
            sudo apt-get update -y
            sudo apt-get install -y openjdk-17-jdk maven mysql-client nodejs npm nginx git

            echo ">>> Building backend..."
            cd backend
            mvn clean package -DskipTests

            echo ">>> Restarting backend..."
            pkill -f 'java -jar' || true
            nohup java -jar target/*-SNAPSHOT.jar > backend.log 2>&1 &

            echo ">>> Building frontend..."
            cd ../frontend
            npm install
            npm run build

            echo ">>> Deploying frontend to Nginx..."
            sudo rm -rf /var/www/html/*
            sudo cp -r dist/* /var/www/html/
            sudo systemctl restart nginx

            echo "✅ Deployment finished!"
